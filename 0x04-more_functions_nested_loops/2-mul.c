#include "main.h"

/**
 * mul - two multiply two integers
 * @a - first integer
 * @b - second integer
 *
 * Return: the result of multiplication on successful execution
 */
int mul(int a, int b)
{
	int mul;
	mul = a * b;

	return (mul);

}
