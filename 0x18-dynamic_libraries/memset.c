#include "main.h"

/**
 * _memset - to set the mem of a string.
 * @s: the string to be considered.
 * @b: the character to set.
 * @n: the number to set.
 *
 * Return: the new string.
 */
char *_memset(char *s, char b, unsigned int n)
{
}
